collections_config:
  posts:
    output: true
    title: Posts
    editor:
      type: markdown
    filename: "{{date}}-{{title | slugify}}"
    fields:
      title:
        type: text
      author:
        type: text
      date:
        type: datetime

media_dir: uploads
